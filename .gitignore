# dependencies
node_modules/

# build output (the compiled binaries are ~80-100 MB)
dist/

# the OpenTUI docs installed locally for reference
.agents/

# platform-tools fetched by `npm run fetch:adb`, and the archive staged from it
assets/platform-tools/
assets/platform-tools.tar.gz

# local artefacts
.DS_Store
screenshot.png
*.log

# python tools (tools/*.py import each other for the pty replay)
__pycache__/
